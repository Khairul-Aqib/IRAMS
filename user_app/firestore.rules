rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This allows ANY logged-in user to read/write ANY document.
    // Perfect for finishing your FYP without fighting permissions.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}